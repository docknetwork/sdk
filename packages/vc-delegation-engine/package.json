{
  "name": "vc-delegation-engine",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r src dist && cp package.json dist",
    "lint": "eslint \"src/**/*.js\"",
    "docs-disabled": "rm -rf out && mkdir out && touch out/.nojekyll && jsdoc src -r -c ../../.jsdoc -d out/reference",
    "test": "vitest run ./tests",
    "prepublishOnly": "yarn build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/docknetwork/sdk"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsonld": "^6.0.0",
    "rify": "^0.7.1"
  },
  "peerDependencies": {
    "@cedar-policy/cedar-wasm": "^4.5.0"
  },
  "devDependencies": {
    "@cedar-policy/cedar-wasm": "^4.5.0",
    "@docknetwork/credential-sdk": "^0.54.9",
    "@vitest/coverage-v8": "^2.0.2",
    "vitest": "^2.1.8"
  }
}
